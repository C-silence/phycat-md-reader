# 安卓 Markdown 阅读器 —— 项目文档

> 一个在 **安卓平板** 上「导入 + 书架管理 + 查看」Markdown 文档的应用。
> 核心目标：**阅读区域的排版、配色、字体复刻 Typora**，并内置 phycat 系列主题、可随时切换。
> 当前工作目录：`D:\桌面\安卓md`（本目录即为项目根，含 phycat 主题源文件）。

---

## 1. 项目定位

| 项 | 定位 |
|----|------|
| 范围 | **纯查看（Viewer）**，不做编辑。用户把已编辑好的 md 发送/拷贝到安卓设备后导入查看 |
| 形态 | "Markdown 书架阅读器"：导入 → 书架列表 → 阅读页 |
| 排版目标 | 阅读页观感 ≈ Typora（换肤后就是 Typora 对应主题的观感） |
| 主题 | 内置 9 套 phycat 主题（8 亮 + 1 暗），随时切换 |
| 平台 | 安卓（优先平板，横竖屏自适应） |

**明确不做（当前阶段）**：编辑、WYSIWYG 光标编辑、云同步、多端。

---

## 2. 关键技术决策（为什么这么设计）

### 决策 1：渲染载体用 WebView，而不是原生组件逐条翻译 CSS

phycat 主题是**为 Typora 的 HTML 结构编写的 CSS**，使用了大量 `#write`、`h1..h6.md-heading`、`pre.md-fences`、`.task-list-item`、`pre.md-meta-block` 选择器，并依靠 CSS 变量、`::before/::after`、内嵌 data:SVG 实现渐变标题、小图标、背景纹理、自动编号。

- ❌ 若用 Compose/Markwon 重绘：这些效果要逐条手写还原，工程量巨大且必然走样。
- ✅ 用 WebView 加载「Markdown→HTML + 原版主题 CSS」：**CSS 原样生效，观感≈Typora**，开发成本低。

因此阅读页 = **WebView**，软件外壳（文件树/工具栏/主题入口）= Compose。

### 决策 2：主题文件原样打包复用

`phycat-*.css` 通过 `@import url(./phycat/phycat.light.css)`（或 `.dark`）引用基础样式，基础样式再按**相对路径**引用字体。所以打包时**必须保持目录结构原样**：

```
安卓md/
├── phycat-caramel.css        （8 个亮色配色 = 各导入 phycat.light.css + 覆盖 :root 变量）
├── phycat-dark.css           （暗色配色 = 导入 phycat.dark.css）
└── phycat/
    ├── phycat.light.css      （基础样式 light）
    ├── phycat.dark.css       （基础样式 dark）
    └── 字体：HarmonyOS_Sans_SC_Regular/Bold.woff、
             SourceHanSerifCN-Medium/Bold.ttf、Cascadia-Code-Regular.ttf
```

切换主题 = **换掉加载的 color css 并刷新**，WebView 会沿 `@import` 链重新拉取。

### 决策 3：正文"纸面"就是 `#write`

基础样式中 `#write { max-width:950px; margin:0 auto; padding:15px; ... }`，背景纹理由 `#write::before`（`mask-image` 叠加）实现。
因此**阅读页的核心就是把 md 渲染出的 HTML 塞进 `<div id="write">`**，其余交给 CSS。

### 决策 4：Markdown 语法 → Typora HTML 结构的适配

主题依赖 Typora 专属类名。WebView 里渲染 md→HTML 后，需对 DOM 做轻量"贴类"（fidelity shim）：

| Markdown 元素 | 需生成的 HTML | 贴上的 Typora 类 | 作用 |
|---|---|---|---|
| 文档正文容器 | `<div id="write">…</div>` | — | 主题几乎所有样式挂在这里 |
| 标题 H1~H6 | `<h1>…</h1>` 等 | `md-heading` | 命中标题图标/装饰 |
| 代码块（围栏） | `<pre><code class="language-xx">` | `pre` 上加 `md-fences` | 代码块边框/角标样式 |
| YAML front-matter | 顶部独立成块 | `pre.md-meta-block` | 元信息块样式 |
| 任务清单 `- [ ]` | `<li class="task-list-item"><input type="checkbox" …>` | `task-list-item` | 复选框样式 |
| 大纲/目录（可选） | 主题自带 `.outline-*` 类样式，可复刻侧栏大纲 | — | 仅在设计侧栏时使用 |

### 决策 5：语言/渲染库

- md→HTML：**markdown-it**（已 vendor 在预览页，WebView 端可换用 Android 侧渲染再拼 HTML，或直接复用同构 HTML）。
- 需要 `html:true`（用户文档里常有内嵌 HTML/图片）。
- 后续可选扩展：`markdown-it-task-lists`、footnote、KaTeX（数学）、highlight 配色。

---

## 3. 目标工程结构（Android，规划）

```
安卓md/
├── 项目文档.md                 ← 本文件
├── theme-preview/              ← 第一步：主题保真度预览（浏览器即可打开）
│   ├── index.html              ← 预览页（= 将来 WebView 加载内容的原型）
│   ├── sample.md               ← 样例文档
│   ├── vendor/markdown-it.min.js
│   └── README.md
├── phycat*.css  / phycat/      ← 主题源文件（勿动，打包时整体引用/复制）
│
└── android/                    ← 第二步起（待初始化，需 Android Studio + SDK）
    ├── app/src/main/assets/
    │   ├── web/                ← 阅读页 HTML + js（可由 theme-preview 迁移而来）
    │   └── themes/             ← 主题 css + phycat/ + 字体（原样复制）
    ├── app/src/main/java/…     ← Kotlin + Compose：外壳/书架/导入/主题切换
    └── …
```

---

## 4. 功能路线图

| 里程碑 | 内容 | 状态 |
|---|---|---|
| **M0 主题预览** | 浏览器预览页：真 md + 9 套主题 + 侧栏（文件/大纲页签） | ✅ 完成（`theme-preview/`） |
| **M1 Android 壳** | WebView 封装 APK：加载阅读页、SAF 导入单文件/整目录、接收外部 md、主题切换 | ✅ 完成（`android/`，产物 `MD阅读器-v0.1-debug.apk`） |
| **M2 文库持久化** | 文件库落盘 + 重启记忆（SAF `takePersistableUriPermission`，文件留在原处） | ⬜ 下一步 |
| **M3 阅读体验** | 平板分栏调优、阅读位置记忆、字号调节、真机字体验证 | ⬜ |
| **M4 可选** | 图片相对路径解析、代码高亮/数学、导出、桌面图标等 | ⬜ |

---

## 5. 风险与开放问题

1. **安装包体积**：字体约 40MB（思源宋体每份 ~14MB）。观感优先→全带；体积敏感→字体子集化。
2. **主题为编辑器编写**：混有 Typora 源码模式才用的 `.cm-*`（CodeMirror）规则，纯查看不命中、无害；但**代码文字配色可能依赖它们**，需要补一层很薄的"查看器专用 override css"垫底。
3. **内嵌图片**：md 中的本地相对路径图片（`![](img/a.png)`）单发文件带不来，需图片目录整体拷入；网络图片可直接加载。
4. **未支持的 md 特性**：脚注、内联数学 `$..$`、mermaid 图，第一版按普通文本/代码处理，后续按需加 KaTeX 等。
5. **复刻边界**：复刻目标是"阅读区观感≈Typora 换肤"，桌面壳（菜单/多标签等编辑器逻辑）不做。
6. **文件类型接收**：安卓对 `.md` 的 MIME 识别差，需要同时声明 `text/plain / text/markdown / application/octet-stream` 的 VIEW+SEND 过滤器（详见对话纪要）。
7. 主题 css 若不希望每次联网拉 CDN 渲染库 → 已 vendor 本地（预览页离线可用）。

---

*最后更新：2026-09-06*
