# 安卓 Markdown 阅读器 · 开发总结（交接/续作手册）

> 面向"之后继续完善 / 加新功能"的完整速览。
> 配套文档：`项目文档.md`（需求/决策/路线图）、`android/README.md`（构建与装板）。
> 续作时先读本文件，再读 `项目文档.md`，即可恢复全部上下文。

---

## 1. 一句话现状

一个 **WebView 封装**的安卓 Markdown **查看器**，复刻 Typora 观感、内置 9 套 phycat 主题、带可折叠侧栏（文件 / 大纲页签），支持 SAF 导入单个或整目录 `.md`、接收其它 App 的"打开/分享"。**第一版可安装 APK 已产出**（`MD阅读器-v0.1-debug.apk`，约 30MB）。

当前状态：M0 ✅ 预览页；M1 ✅ 安卓壳 APK；M2（文库持久化）⬜ 未做。

---

## 2. 目录总览（截至现在）

```
D:\桌面\安卓md\
├── 项目文档.md               需求、技术决策、路线图 M0~M4
├── 开发总结.md               本文件
├── phycat-caramel.css … sky.css  9 套配色主题（css）
├── phycat/                   基础样式 light/dark + 字体（勿拆散）
├── theme-preview/            ★ 阅读页原型（浏览器直接双击可看）
│   ├── index.html            渲染 + 侧栏 + 换肤（唯一逻辑源）
│   ├── sample.md             样例文档（index.html 内嵌一份同内容副本）
│   ├── vendor/markdown-it.min.js  本地化渲染库（离线）
│   ├── README.md
│   └── shots/*.png           历次无头浏览器实测截图
├── android/                  ★ 安卓工程
│   ├── README.md             构建 / 安装 / 桥接说明
│   ├── gradle.properties     （含 android.overridePathCheck=true）
│   ├── local.properties      sdk.dir（本机路径）
│   ├── tools/sync_assets.py  ★ 把原型同步进 assets 的一键脚本
│   └── app/src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/example/mddroid/MainActivity.java  安卓壳 + 桥
│       └── assets/app/       由 sync_assets.py 生成的网页资源
└── downloads/phycat-md-reader-v0.1-debug.apk  本地已签名 APK 暂存（不入库；对外分发走 GitHub Release）
```

**维护铁律**：网页逻辑只改 `theme-preview/index.html` 一份；安卓用 `python android/tools/sync_assets.py` 同步（自动把 css 路径 `../phycat-` 改写成同目录 `phycat-`），再重新构建。

---

## 3. 核心设计（续作前必须理解）

### 3.1 为什么能"复刻 Typora"
phycat 主题就是 **CSS**，且样式几乎全挂在 `<div id="write">` 下（`#write h1..`、`hN.md-heading`、`pre.md-fences`、`.task-list-item`、`pre.md-meta-block`）。所以阅读区 = WebView 里渲染 md→HTML 后**包进 `#write`，CSS 原样生效**，观感≈Typora，切换主题 = 换 `<link>` 指向的 css。

### 3.2 fidelity shim（网页侧，最关键的小逻辑）
markdown-it 输出的 HTML 与 Typora 的 DOM 不同，`decorate()` 负责贴类名：
- 标题 → 加 `md-heading`（命中图标/装饰）
- 代码块 `<pre>` → 加 `md-fences`
- front-matter → 剥出渲染成 `pre.md-meta-block`
- `- [ ] / - [x]` 列表 → 转 `li.task-list-item` + `<input type=checkbox>`

新增渲染能力时先想"这元素该长成 Typora 的哪种 DOM"，再在 `decorate`/`fixTasks` 附近加 shim。

### 3.3 侧栏"跟随主题"的坑（已修，别回退）
默认配色变量声明在 **`body`** 上会盖住 `<html>` 的覆盖值。已改成：默认变量放 **`:root`**，`body` 只保留字体与 dark-chrome 的底色覆盖；切主题时 JS `applyAccent()` 读取当前主题 `:root` 里的 `--element-color`/`--head-title-color` 写回 `--side-acc/--acc-soft/--acc-soft2`，让选中项/大纲高亮/页签下划线随主题变色。

### 3.4 原生 ⇄ 网页桥（双端约定，勿只改一边）
| 方向 | 约定 |
|---|---|
| 网页 → 原生 | 按钮检测 `window.AndroidBridge.pickFile()/pickFolder()`；浏览器回退本地 input |
| 原生 → 网页 | 调 `window.mdReader.addFiles([{name,path,text},…])` 入库并打开 |
| 外部接收 | `MainActivity.handleIncoming` 处理 VIEW / SEND，读完后经同接口注入 |

`MainActivity.java` 里单文件用 SAF `ACTION_OPEN_DOCUMENT`；文件夹用 `ACTION_OPEN_DOCUMENT_TREE` + `DocumentsContract` 递归收 `.md`（上限 8 层 / 300 个）。manifest 已注册 VIEW/SEND intent-filter 接收 md。

---

## 4. 构建环境与重打包 runbook

### 本机已就绪的工具链
| 项 | 位置 / 值 |
|---|---|
| JDK21 | `D:/software/java/jdk21`（**用这个**，JAVA_HOME 别指 jdk24） |
| Android SDK | `C:/Users/12474/AppData/Local/Android/Sdk`（platform **android-36**、build-tools 35/36、licenses 已接受） |
| Gradle | `C:/Users/12474/buildtools/gradle/gradle-8.11.1`（从腾讯镜像下载的独立版，不在 PATH） |
| AGP | 8.10.1（首次构建自动拉取） |

> 教训：`services.gradle.org` 极慢（≈30KB/s），改走腾讯镜像 `https://mirrors.cloud.tencent.com/gradle/…` 后 11MB/s。后续要换 Gradle 版本同样用它。

### 完整重打包流程
```bash
# 1. 改原型（只改 theme-preview/index.html 等）
# 2. 同步进 assets（自动处理 css 路径）
python android/tools/sync_assets.py

# 3. 构建（工程路径含中文，gradle.properties 已加 overridePathCheck=true）
cd android
export JAVA_HOME=/d/software/java/jdk21
export ANDROID_HOME="C:/Users/12474/AppData/Local/Android/Sdk"
"C:/Users/12474/buildtools/gradle/gradle-8.11.1/bin/gradle" assembleDebug

# 4. 产物（本地暂存，不入库）
cp app/build/outputs/apk/debug/app-debug.apk ../downloads/phycat-md-reader-v0.1-debug.apk
# 5.（可选）发布到 GitHub Release：
#    gh release upload v0.1.0 ../downloads/phycat-md-reader-v0.1-debug.apk
```

### 装到平板 / 真机验证
无设备在线时：把 apk 传到平板点装（允许"未知来源"）。有设备时：
```bash
"$ANDROID_HOME/platform-tools/adb.exe" install -r android/app/build/outputs/apk/debug/app-debug.apk
"$ANDROID_HOME/platform-tools/adb.exe" shell am start -n com.example.mddroid/.MainActivity
```

---

## 5. 已验证 / 已知限制

**已验证**：9 主题渲染、标题自动编号、表格/代码/任务清单/front-matter/图片、暗色联动、侧栏折叠与页签记忆、大纲跳转与滚动高亮、侧栏强调色跟随主题、原生注入接口。**未真机验证**字体/滚动/暗色在真实平板的观感。

**已知限制（后续完善候选）**
1. **文库不持久**：重启回到内置示例；文件内容只读进内存。→ M2：SAF `takePersistableUriPermission` + 记录授权与列表，文件留在原处。
2. 代码块无逐语言高亮（只吃主题底色）；`md-fences` 内含 CodeMirror `.cm-*` 仅编辑器专用，未命中无害。
3. 脚注 `[^1]`、数学 `$..$`、mermaid 图未渲染。
4. md 内**本地相对路径图片**在 WebView 里无法直接解析（网页副本同理），需 M4 解决：读 md 时把同目录图片转 data URI 或临时落地。
5. 安装包 30MB（字体全内置）；正式分发需自备签名（目前 release 也走 debug 签名）。
6. 应用无桌面图标资源、横竖屏靠 manifest `configChanges` 兜底而非自适应布局。

---

## 6. 接下来可做的路线（对应 `项目文档.md` 的 M2~M4）

- **M2 文库持久化（优先级最高）**：文件夹授权记住（重启不用重选）、文件列表持久化、断连文件处理、多文库。
- **M3 阅读体验**：字号调节注入、阅读位置记忆、真机字体微调、平板双栏排版打磨。
- **M4 内容能力**：KaTeX/mermaid/highlight 逐项加（沿用"网页渲染 + fidelity shim"思路，改动集中在 theme-preview/index.html + vendor），图片本地化。
- 其它顺手项：桌面图标、去掉 top 调试栏改成 App 自己的顶栏、把"大纲"改为右侧抽屉、深/亮色跟随系统。

**新增一套主题的步骤**（如拿到新的 Typora .css）：
1. 放进项目根（若它 @import 基础/字体，把 phycat 基础结构一并处理）。
2. 在 `theme-preview/index.html` 的 `THEMES` 数组加一条 `{file,name,outer,dark}`。
3. `python android/tools/sync_assets.py`（脚本只复制脚本里列的白名单，新文件要加进列表或手动 cp）→ 重新构建。

---

*文档更新：2026-09-07 · 接续工程时，可让 Claude 先读 `开发总结.md` 与 `项目文档.md` 再动手。*
